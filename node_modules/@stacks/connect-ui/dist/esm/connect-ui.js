import { p as promiseResolve, b as bootstrapLazy } from './index-0ffade8e.js';

/*
 Stencil Client Patch Browser v2.15.0 | MIT Licensed | https://stenciljs.com
 */
const patchBrowser = () => {
    const importMeta = import.meta.url;
    const opts = {};
    if (importMeta !== '') {
        opts.resourcesUrl = new URL('.', importMeta).href;
    }
    return promiseResolve(opts);
};

patchBrowser().then(options => {
  return bootstrapLazy([["connect-modal",[[1,"connect-modal",{"authOptions":[16],"hasOpenedInstall":[32]}]]]], options);
});
